# ~/.config/.zshenv
# ------------------------------------------------------------
# Note: This configuration file is sourced only when zsh is launched as a login shell using the --login flag.
# ------------------------------------------------------------
# 
# 引入环境变量
[ -f "$HOME/.config/zsh/zsh_env" ] && source "$HOME/.config/zsh/zsh_env"
